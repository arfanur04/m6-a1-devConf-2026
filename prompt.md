# Prompt

Generate a beginner‑friendly FAQ section for a developer conference website.  
Use only HTML with inline CSS styles (no external stylesheets or `<style>` blocks).  
Include 4–5 common questions with collapsible answers using `<details>` and `<summary>`.  
Keep the design very simple, with inline styles for font, margins, borders, and bold text
